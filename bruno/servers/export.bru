meta {
  name: Export Servers (Success)
  type: http
  seq: 37
}

get {
  auth: bearer
  url: {{baseURL}}/api/v1/servers/export?page=1&limit=100
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 200
  res.body.fileContent: isString
  res.body.filename: isString
}
