meta:
  name: View Servers - Sort DESC
  type: http
  seq: 24

http:
  method: GET
  url: "{{baseURL}}/api/v1/servers?page=1&limit=10&sort_by=server_name&sort_direction=desc"

assert:
  - res.status == 200
