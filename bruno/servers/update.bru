meta {
  name: Update Server (Success)
  type: http
  seq: 26
}

put {
  url: {{baseURL}}/api/v1/servers/{{createdServerId}}
  body: json
}

body:json {
  {
    "server_name": "{{createdServerName}}-updated",
    "ipv4": "{{testServerIPv4Alt}}"
  }
}

assert {
  res.status: eq 200
  res.body.server.server_id: eq {{createdServerId}}
  res.body.server.ipv4: eq {{testServerIPv4Alt}}
}
