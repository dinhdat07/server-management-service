meta {
  name: Create Server - Duplicate IPv4 (409)
  type: http
  seq: 13
}

post {
  url: {{baseURL}}/api/v1/servers
  body: json
}

body:json {
  {
    "serverName": "unique-name-{{$randomInt}}",
    "ipv4": "{{testServerIPv4}}"
  }
}

assert {
  res.status: eq 409
}
