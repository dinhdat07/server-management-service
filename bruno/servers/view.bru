meta {
  name: View Servers (Default)
  type: http
  seq: 19
}

get {
  url: {{baseURL}}/api/v1/servers?page=1&limit=10
}

assert {
  res.status: eq 200
  res.body.total_count: gte 0
  res.body.servers: isArray
}

script:post-response {
  bru.setVar("totalServers", res.body.total_count);
}
