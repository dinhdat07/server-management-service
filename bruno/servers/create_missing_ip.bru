meta:
  name: Create Server - Missing IPv4 (400)
  type: http
  seq: 16

http:
  method: POST
  url: "{{baseURL}}/api/v1/servers"
  body: json

body:json:
  {
    "server_name": "no-ip-server"
  }

assert:
  - res.status == 400
