meta {
  name: Create Server - Missing IPv4 (400)
  type: http
  seq: 16
}

post {
  url: {{baseURL}}/api/v1/servers
  body: json
}

body:json {
  {
    "serverName": "no-ip-server"
  }
}

assert {
  res.status: eq 400
}
