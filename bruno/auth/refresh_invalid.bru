meta:
  name: Refresh - Invalid Token (401)
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseURL}}/api/v1/auth/refresh"
  body: json

body:json:
  {
    "refresh_token": "this-is-not-valid"
  }

assert:
  - res.status >= 400
